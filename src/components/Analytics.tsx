import { useEffect } from 'react';

export function Analytics() {
  useEffect(() => {
    // Intentionally quiet for local development until a real analytics endpoint is added.
  }, []);

  return null;
}
